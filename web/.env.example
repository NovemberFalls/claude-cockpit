# Server config
HOST=0.0.0.0
PORT=8420

# Maximum concurrent sessions (default: 8)
MAX_SESSIONS=8

# Idle session timeout in seconds (default: 0 = disabled)
IDLE_TIMEOUT=0

# Suppress auto-opening browser (set to 1 to disable)
NO_BROWSER=0
