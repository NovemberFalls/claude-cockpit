{
  "identifier": "default",
  "description": "Default capabilities for Claude Cockpit",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
